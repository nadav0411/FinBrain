# FinBrain Project - keep-alive-backend.yml - MIT License (c) 2025 Nadav Eshed


name: Keep FinBrain Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *" # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render backend
        run: |
          echo "Pinging backend..."
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" https://finbrain.onrender.com/health || true)
          if [ "$RESPONSE" = "200" ]; then
            echo "Backend is alive and responding with 200 OK 🟢"
          else
            echo "Ping failed or returned status code: $RESPONSE 🔴"
            exit 1
          fi
